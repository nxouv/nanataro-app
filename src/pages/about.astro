---
import BaseLayout from '../layouts/BaseLayout.astro';
import BackLink from '../components/BackLink.astro';
import PageHeader from '../components/PageHeader.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="About" description="なな太郎について">
  <main class="container">
    
    <BackLink href="/" label="ホーム" />
    <PageHeader title="About" subtitle="もう少し詳しい自己紹介" />

    <div class="about-content">
      
      <section class="about-section">
        <div class="about-profile">
          <div class="about-icon">
            <img src="/icon.png" alt="">
          </div>
          <div>
            <div class="about-name">なな太郎</div>
            <div class="about-id">@nxouv</div>
          </div>
        </div>
        <div class="about-text">
          <p>アプリは自然言語だけで書いてます。</p>
          <p>なんでもすぐに飽きてしまうので、<br>数日でできることを考えてやるようにしてます。</p>
          <p>個別的なものがすきです。特別でなくていいです。</p>
        </div>
      </section>

      <section class="about-section">
        <h2 class="about-section-title">連絡先</h2>
        <div class="contact-list">
          <div class="contact-item">
            <span class="contact-label">X</span>
            <span class="contact-value"><a href="https://x.com/nxouv" target="_blank" rel="noopener noreferrer">@nxouv</a></span>
          </div>
          <div class="contact-item">
            <span class="contact-label">Email</span>
            <span class="contact-value"><a href="mailto:nanataro.nx@gmail.com">nanataro.nx@gmail.com</a></span>
          </div>
          <div class="contact-item">
            <span class="contact-label">GitHub</span>
            <span class="contact-value"><a href="https://github.com/nxouv" target="_blank" rel="noopener noreferrer">nxouv</a></span>
          </div>
        </div>
      </section>

      <section class="about-section">
        <h2 class="about-section-title">使っているもの</h2>
        <div class="uses-list">
          <span class="uses-item">自作PC</span>
          <span class="uses-item">インターネット</span>
          <span class="uses-item">カメラ</span>
          <span class="uses-item">線香</span>
          <span class="uses-item">学習タイマー</span>
          <span class="uses-item">ウォーキングシミュレーター</span>
        </div>
      </section>

    </div>

    <Footer />

  </main>
</BaseLayout>

<style>
  /* About Content */
  .about-content {
    margin-bottom: var(--space-16);
  }

  .about-section {
    margin-bottom: var(--space-12);
  }

  .about-section:last-child {
    margin-bottom: 0;
  }

  .about-section-title {
    font-size: var(--text-xs);
    font-weight: 500;
    color: var(--color-text-subtle);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    margin-bottom: var(--space-4);
  }

  /* Profile */
  .about-profile {
    display: flex;
    align-items: flex-start;
    gap: var(--space-5);
    margin-bottom: var(--space-6);
  }

  .about-icon {
    width: 72px;
    height: 72px;
    border-radius: var(--radius-full);
    background: linear-gradient(135deg, var(--color-gradient-start) 0%, var(--color-gradient-end) 100%);
    overflow: hidden;
    flex-shrink: 0;
  }

  .about-icon img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .about-name {
    font-size: var(--text-2xl);
    font-weight: 500;
    color: var(--color-text-primary);
    margin-bottom: var(--space-1);
  }

  .about-id {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .about-text {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  .about-text p {
    margin-bottom: var(--space-3);
  }

  .about-text p:last-child {
    margin-bottom: 0;
  }

  /* Contact */
  .contact-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .contact-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    font-size: var(--text-base);
  }

  .contact-label {
    color: var(--color-text-muted);
    min-width: 60px;
  }

  .contact-value a {
    color: var(--color-link);
    transition: opacity var(--transition-base);
  }

  .contact-value a:hover {
    opacity: 0.7;
  }

  /* Uses */
  .uses-list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .uses-item {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    background-color: var(--color-border);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-xl);
  }

  @media (max-width: 640px) {
    .about-profile {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-4);
    }

    .about-icon {
      width: 64px;
      height: 64px;
    }
  }
</style>